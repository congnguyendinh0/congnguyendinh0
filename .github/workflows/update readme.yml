name: Update README

on:
  schedule:
    - cron: '*/30 * * * *'
  workflow_dispatch:

jobs:
  build:
    name: readme
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - uses: congnguyendinh0/github-activity-readme@master
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
